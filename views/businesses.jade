extends layout

block content
  h1 Business Listing

  div
    table(class="table table-hover")
      each businessName in listing
        tr
          td(href=businessName.id) 
            a(href= '/businesses/' + businessName.id)= businessName.name
          td= businessName.email
          td= businessName.address
          td= businessName.phone